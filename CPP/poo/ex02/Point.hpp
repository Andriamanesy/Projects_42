#ifndef POINT_H
#define POINT_H

#include <iostream>

class Point
{
    public:
        Point();
        Point(int x, int y);
        Point(const Point& p);
        ~Point();

    private:

};

#endif